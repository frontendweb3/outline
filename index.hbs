{{!< default}} 

{{> "hero"}}

{{> "tags"}}

<section class="mx-auto max-w-7xl px-4 md:px-8 py-10">

    {{> "posts-grid"}}

    {{#match @custom.pagination_style "simple"}}
        {{> "simple-pagination"}}
    {{else}}
        {{> "pagination"}}
    {{/match}}
</section>